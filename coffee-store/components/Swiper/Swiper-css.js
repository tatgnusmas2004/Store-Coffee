import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  part3: {
    width: '100%',
    height: 200,
    marginBottom: 20,
  },
  slide: {
    width: '95%',
    height: '100%',
    paddingLeft: 15,
  },

  sliderImg: {
    width: '100%',
    height: '100%',
    resizeMode: 'cover',
    borderRadius: 15,
  },
});
export default styles;